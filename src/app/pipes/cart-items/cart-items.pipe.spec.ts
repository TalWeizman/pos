import { CartItemsPipe } from './cart-items.pipe';

describe('CartItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CartItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
